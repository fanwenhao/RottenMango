<h1>Rotten Mangoes</h1>
<hr>
<% @users.each do |user| %>
    <div>
      ID#<%= user.id %> , <%= user.full_name %> , <%= link_to 'edit', edit_admin_user_path(user.id) %> , <%= link_to 'delete', admin_user_path(user.id), method: :delete, data: { confirm: "You sure?" } %>
    </div>
<% end %>
